# MOC - University

## Schedule
- [[Semester 1 2026 - Course Schedule]]

## Current
```dataview
LIST
FROM "Efforts/Active"
WHERE contains(file.name, "CSX") OR contains(file.name, "ITX") OR contains(file.name, "BBA") OR contains(file.name, "ELE") OR contains(file.name, "GE") OR contains(file.name, "University") OR contains(file.name, "Report") OR contains(file.name, "Exam")
SORT file.name ASC
```

## Archive
```dataview
LIST
FROM "Efforts/Archive"
SORT file.name ASC
```
